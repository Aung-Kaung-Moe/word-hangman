# word-hangman
### Place words.txt in the same directory as Main.java and run below command in window powershell:
cmd /c "chcp 65001>nul && javac -encoding UTF-8 Main.java && java -Dfile.encoding=UTF-8 Main"
